import { describe, expect, it } from "vitest";

// Placeholder proving the runner picks up this workspace. Replaced by real
// tests when the package gets its first module.
describe("@nap/db", () => {
  it("is wired into the test runner", () => {
    expect(true).toBe(true);
  });
});
