{
  "extends": "../../tsconfig.base.json",
  // `scripts` is included so the CLI harness is typechecked like everything else — a
  // script outside the gates is exactly how test/ once shipped unchecked.
  "include": ["src", "scripts"]
}
