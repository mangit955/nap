apiVersion: apps/v1
kind: Deployment
metadata:
  name: nap-reaper
  namespace: nap
spec:
  template:
    spec:
      containers:
        - name: reaper
          command: ["bun", "apps/api/scripts/cluster-proof.ts", "reaper"]
          imagePullPolicy: Never
