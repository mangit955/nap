# Nap v1 Progress

Current milestone: **M3 — Presentation & Streaming — COMPLETE** (branch `feat/m3-presentation`).
Next up: **M4 — Persistence**. M0 and M1 are merged to `main`; M2 and M3 are on their branches,
with M2-1 and M2-9 still `IN_PROGRESS` pending one real run each.

> **The app works end to end as of M3-7.** `bun run dev` with a Postgres running serves a page
> that creates its own session, sends a message, streams the turn, shows the preview and lists
> the files. The API now spends money: it creates E2B sandboxes and calls the model, defaulting
> to `claude-sonnet-5` at `medium` — see `apps/api/.env.example`.

> Before any M2 work, read the `claude-api` skill rather than answering from memory —
> `CLAUDE.md` explains why that milestone is the one where a stale recollection costs most.

> **Which model to point at.** Iterate on `claude-sonnet-5`; record anything anyone will
> watch on `claude-opus-5`. Same SDK, same tool-call and streaming shapes, same refusal
> semantics — the only differences are quality and effort tuning, which is what makes this
> safe where a second *vendor* would not be. See the model-split gotcha in `CLAUDE.md`.
> **Actionable since M2-9:** `ClaudeProvider` takes `model`, `effort` and `maxTokens`;
> `DEFAULT_MODEL_CONFIG` still holds the Opus/`xhigh` defaults, and `bun run harness --real`
> overrides them with the cheap ones.

## How to use this file

This file tracks **status only**. What each task actually means is in `docs/PLAN.md` §4,
under the matching ID; how to work in this repo is in `CLAUDE.md`.

Pick the next task whose status is `TODO` **and** whose `Deps` are all `DONE`.
`Deps` are transcribed from `docs/PLAN.md` §4 — if the two ever disagree, the plan wins.

Statuses: `TODO` · `IN_PROGRESS` · `DONE` · `BLOCKED` (with reason) · `SKIPPED` (with reason).

## Tooling & infrastructure

Not `docs/PLAN.md` §4 tasks — repo tooling added alongside the milestones, tracked
here so it isn't mistaken for product work.

| ID | Task | Status | Notes |
|----|------|--------|-------|
| T-1 | Dependency-direction test | DONE | `test/architecture.ts`. PLAN.md §0 called the direction "enforced" but nothing enforced it; now a test does, including "agent must not depend on e2b". Verified by injecting a real violation. |
| T-2 | Hook blocking bare `bun test` | DONE | `.claude/settings.json` PreToolUse. |
| T-3 | `nap-session` skill | DONE | `.claude/skills/nap-session/` — automates the §1 protocol. |
| T-4 | `nap-events` skill | DONE | `.claude/skills/nap-events/` — event test discipline. Updated at M0-3 with the concrete 11-type table and the two jsonb rules (no `Date`/`undefined`, strict payloads). |
| T-5 | GitHub Actions CI | DONE | `.github/workflows/ci.yml` — lint/typecheck/test on push to main + `feat/**`, and on PRs. Integration suite deliberately excluded (costs real spend). |
| T-6 | Permission allowlist | DONE | `.claude/settings.json`. Only `bun run <script>` exact forms — everything else we run (git/gh read-only, jq, rg, grep) is already auto-allowed. No `bun run *` wildcard (arbitrary code execution) and no `test:integration` (the prompt is the only checkpoint before real spend). |
| T-7 | Auto-format on write | DONE | PostToolUse `Write\|Edit` → `biome check --write` on the touched file. Removes the write→lint-fails→format→retry loop. |
| T-8 | Dirty-tree Stop hook | DONE | Warns (does not block) when the tree is dirty at session end — mechanises §1's "never end a session with uncommitted work". |
| T-9 | PLAN↔PROGRESS consistency test | DONE | `test/docs.ts`. The Deps column is a hand transcription of PLAN.md §4; this stops the two silently disagreeing. Verified by injecting drift. |
| T-10 | "Definition of done" gate | DONE | Five-point gate in `CLAUDE.md`, executable form in `nap-session` finish. The retroactive audit that produced it found a real bug: `test/` sat outside typecheck for two commits (T-1, T-9 both shipped unchecked), because package tsconfigs only include `src`. Fixed by a root `tsconfig.json` + `tsc --noEmit` appended to the typecheck script; lefthook and CI inherit it. Audit re-verified T-1…T-9 otherwise sound. |
| T-11 | No-task-IDs-in-source test | DONE | `test/comments.ts`. Task IDs in comments read as tracker residue to anyone outside the project — they date the code instead of explaining it. Found 3 pre-existing cases from M0-1 plus 4 of my own in M0-3, so it was already a habit. Plan *section* refs (`docs/PLAN.md §5`) stay legal. Verified by injecting a real violation. |
| T-12 | Bedrock platform path | DONE | `@anthropic-ai/bedrock-sdk` behind the same narrow `AnthropicClient` seam M2-1 introduced, so `ClaudeProvider`, the agent loop and the event contract are unchanged — only the client construction and an `anthropic.` model-id prefix differ. Exists because Anthropic-direct billing was unavailable; Bedrock serves the same models through an AWS account. `bun run harness --real --platform=bedrock`. **Two things only running it revealed:** the client **throws at construction** when no region resolves (so the harness checks `AWS_REGION` up front), and `process.env.X = undefined` stores the string `"undefined"` — the first version of that test passed vacuously. The provider integration test now runs against whichever account is configured rather than requiring an Anthropic key. Architecture guard extended and verified by injecting a violation. **Verified against real Bedrock:** the bearer key authenticates, region resolution works, and `anthropic.claude-opus-5` is the correct id form — the API returns structured errors our provider maps correctly. **Blocked on AWS, not on code:** every current model answers `403 "is not available for this account"` in `us-east-1`, and `404` in `ap-south-1` (that endpoint serves no Claude models). Legacy versioned ids (`…-v2:0`) 404 everywhere — they belong to the classic bedrock-runtime catalog, not this endpoint. Model access has to be granted in the Bedrock console before any real call can run. |

## M0 — Scaffold & Contracts

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M0-1 | Workspace scaffold | — | DONE | Bun replaces pnpm (see PLAN.md "Bun/Node split"). Vitest kept — run it as `bun run test`, never `bun test`. Packages resolve to `.ts` source via subpath exports, so no build step for test/typecheck; needs `allowImportingTsExtensions`. |
| M0-2 | `CLAUDE.md` + `PROGRESS.md` | M0-1 | DONE | PROGRESS.md was already seeded during M0-1. Added a `Deps` column so this file alone answers "what's next?", as §1 requires. |
| M0-3 | Event schemas | M0-1 | DONE | zod 4.4.3 (`z.iso.datetime`, `z.strictObject`). Envelope nests `payload` to mirror the `events` row. `createdAt` is an ISO **string** and payloads are strict — both are jsonb survival rules, both proven by breaking them. IDs were left as non-empty strings pending the id format; M0-5 settled on `uuid` and tightened them. |
| M0-4 | Interface declarations | M0-3 | DONE | 8 ports in `packages/shared/src/ports/` + a shared `Result` for expected failures. Two traps found: `*.test-d.ts` files were not collected at all (a deliberately wrong assertion passed) until a `types` project + `tsconfig.test-d.json` were added; and `Omit<NapEvent,"seq">` silently flattens the union, decorrelating `type` from `payload` — needs a distributive omit. Also set `"types": ["node"]` in `tsconfig.base.json`; @types/node was not being auto-included, so `AbortSignal` did not resolve. |
| M0-5 | DB schema + migrations | M0-1 | DONE | drizzle-orm 0.45.2 + postgres.js, migrations committed under `packages/db/drizzle/`. **Deviates from §5:** `events` gets a `turn_id` column — §5 sketches the tables, the event union is the contract and carries `turnId`. IDs are `uuid`, so M0-3's `events.ts` tightened to `z.uuid()`. **`created_at` is `timestamptz` but the contract types `createdAt` as an ISO string** — whatever implements `EventStore` must map with `.toISOString()`; there is a test proving that mapping yields a valid `NapEvent`. New `db` vitest project (one container per run, `*.db.test.ts`); `bun run test:fast` is the Docker-free loop. `projects.status` values are provisional — M4-4 owns that vocabulary. |
| M0-6 | API skeleton + env validation | M0-1 | DONE | Hono 4.13.1 + pino 10.3.1. Required env is only what the API reads today (`DATABASE_URL` + three defaulted keys); later keys sit commented in `.env.example` and become required in the task that reads them. Log context is `AsyncLocalStorage`, so `sessionId`/`turnId` reach code the M0-4 ports give no logger to. **pino is fine under Bun** — plain JSON to a stream, no transports (transports use worker threads; don't add one). **Gotcha found:** turbo's strict env mode meant `DATABASE_URL=… bun run dev` failed until `dev` got a `passThroughEnv`; the normal path is a `.env` file, which Bun auto-loads. Boot failure prints and exits 1 rather than throwing a Zod stack trace. |
| M0-7 | Web skeleton | M0-1 | DONE | **Next 16, not 15** — PLAN.md amended; App Router is unchanged, the "15" predated 16. Tailwind v4 (CSS-first, `@theme` in `globals.css`; Biome needs `css.parser.tailwindDirectives`). New `web` vitest project (jsdom + `@vitejs/plugin-react`, which must live in the *root* devDeps since the root config imports it). **Closed two live gaps: `.tsx` matched no vitest glob, and `test/comments.ts` was blind to every `.tsx` file.** `next-env.d.ts` is gitignored — it references `.next/` artifacts CI never builds, and typecheck is green without it. Render tests query by role + accessible name; that caught four `banner` landmarks from `<header>` inside each pane. |

## M1 — Execution Plane

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M1-1 | `SandboxManager` interface + `InMemorySandboxManager` | M0-4 | DONE | The interface itself already landed in M0-4, so this was the fake + the conformance suite (`packages/sandbox/src/testing/`). **Two contract decisions M1-2 must honour:** post-`destroy` ops return `destroyed`, never `not_found` — the E2B adapter needs to track ids it killed, since E2B alone can't tell the two apart; and `listFiles` returns *direct children only*, with directories synthesized. The suite is parameterised by a harness supplying `root` and two concrete shell commands, because it cannot assume a shell. Unscripted `exec` **throws** rather than returning a bland success — a test running an unscripted command asserts on nothing. Verified by breaking the fake three ways and watching the matching cases fail. |
| M1-2 | E2B adapter | M1-1 | DONE | e2b 2.38.0. **Conformance suite passes 13/13 against real E2B; the orphan check (`Sandbox.getInfo` per created id) is green.** The SDK is injected as a narrow `E2BClient` so error-mapping tests run against a stub with no network — no `vi.mock` needed, contrary to the "Bun/Node split" note in `docs/PLAN.md`. Three divergences found and handled, now in `CLAUDE.md`: `commands.run` throws on non-zero exit; `AuthenticationError` doesn't extend `SandboxError`; an arbitrary sandbox id is a 400, not a 404 — which forced a new `unknownSandboxId()` on the harness, since the suite cannot invent a well-formed id. Handles are cached per sandbox: without it every file op was a fresh `connect` round-trip. `E2B_API_KEY` is read by the integration suite only, so it stays out of `apps/api/src/env.ts` until the server builds a SandboxManager. |
| M1-3 | Project template | M1-2 | DONE | **Cold start (create → sandbox usable): 0.77s / 0.79s / 0.85s / 1.14s over four runs** — dependencies are baked in, so nothing installs at creation. Template `nap-vite-react` (id `uz50ssm54r8rc9n6isll`), built by hand via `bun run template:build`; it is **opt-in** (`new E2BSandboxManager({ template: NAP_TEMPLATE })`), so the M1-2 conformance run stays on the base image. Vite 8 + React 19.2.8 + Tailwind 4.3.3 + Bun. **Four bugs the integration suite caught, none visible locally:** the first build committed all of `node_modules` (no `.gitignore`); the second swept `.profile` and Bun's cache into the repo because the project was rooted at `$HOME` — hence `TEMPLATE_WORKDIR=/home/user/app`; the NodeSource Node 24 install was shadowed by the base image's 20.9 on PATH and did nothing; and the starter app had two type errors. Also relaxed the conformance exec-streaming assertion — cross-stream chunk ordering is not something a real transport promises, and asserting it failed ~1 run in 3. |
| M1-4 | Dev server boot + preview URL | M1-3 | DONE | **Create → preview serves 200: 1.93s / 2.05s / 2.56s / 2.62s** (cold start alone is ~0.9–1.3s; the rest is Vite booting). Verified in a real browser. The template gained `setStartCmd('cd /home/user/app && bun run dev', waitForPort(5173))` as its terminal builder call, so E2B supervises the process and holds `create` open until the port binds. **Added `waitForPreview` to the `SandboxManager` port** — `waitForPort` only proves something bound the port *inside* the sandbox, and the user loads it through a public proxy that becomes ready separately; it polls for a 200 and returns the existing typed `timeout` otherwise. Conformance covers only the timeout and destroyed paths, because it runs against a bare sandbox where success is not observable; the success path lives in the fake's tests and the template integration test. Fake gained `listen(sandboxId, port)` so downstream tests can drive both outcomes without a network. |
| M1-5 | Git helpers | M1-1 | DONE | `commitAll` / `currentSha` / `bundle` / `restoreBundle` in `packages/sandbox/src/git.ts`, free functions over a `SandboxManager`. Round trip verified against real git. **Commit messages are shell-quoted** — they come from a model and are interpolated into a command line, so `$(…)`/backticks/quotes are command injection into the user's sandbox; `shellQuote` is tested by round-tripping ten hostile inputs through a real `sh`, which caught what a naive double-quote version lets through. `git clean -fd` deliberately **without `-x`**, or a restore would delete the baked `node_modules` and turn a 1s open into a reinstall — there is a test for that. No-changes commits are a no-op because read-only turns must not fail. **Known edge:** bundles move as base64 through `exec` stdout (the port has no binary channel), which inflates 33% — fine at v1 sizes, but M4-1 snapshots real projects to R2 and may need `readFileBytes` on the port instead. |

## M2 — Intelligence Plane

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M2-1 | `LLMProvider` + `ScriptedLLMProvider` | M0-4 | IN_PROGRESS | @anthropic-ai/sdk 0.116.0, now exclusive to `@nap/agent` in `test/architecture.ts`. **The M0-4 port could not express the task and was extended:** `LLMRequest` gained `tools` (a model never told about a tool cannot return the `toolCalls` the result already declared) and `LLMMessage.content` widened to a block union so tool results can be fed back; `complete()` moved onto a per-turn handle from `startTurn()`, which makes "usage resets per turn" structural rather than a reset call to forget. **Refusal is typed-only — no server-side `fallbacks`/beta headers** (user decision); M2-7 maps it to `turn.failed`. **`usage.input_tokens` is the uncached remainder**, so cached tokens are summed in — see CLAUDE.md. SDK retries disabled (`maxRetries: 0`) so our own N-attempt policy is the only one and is testable. Everything else is done and all three gates are green. **next: put `ANTHROPIC_API_KEY` in `apps/api/.env` and run `bun run test:integration`** — `claude-provider.integration.test.ts` is written and collected but has never executed, so the real request shape (model id, `xhigh` effort, adaptive thinking, tool schema) is unverified; the stub agrees with anything. Mark DONE once it passes. |
| M2-2 | `MemoryProvider` + `NoopMemoryProvider` | M0-4 | DONE | The interface already landed in M0-4, so this was the no-op plus its proof of inertness. Lives in `packages/context` — the plan puts the call sites in `ContextEngine` (§0 diagram, and the v2 memory row). **Inertness is asserted in both directions:** `retrieve` returns `[]` *and* a consumer given the no-op produces byte-identical output to one given no provider at all; a third test shows the same consumer *does* change when a provider returns something, so the parity assertions cannot pass vacuously. Verified by breaking the class both ways — a leaking `retrieve` fails 5 tests, a throwing `write` fails 5. `retrieve` returns `[]` rather than anything nullable so no call site has to null-check the seam. |
| M2-3 | `ContextEngine` | M2-2, M0-3 | DONE | **The M0-4 port could not express the task and was extended:** `ContextRequest` gained `history: StoredEvent[]` — §4 asks for a conversation window and nothing in the request was a source of one. Passed as data rather than an `EventStore` so assembly does no I/O and the truncation tests run on literal arrays. **Truncation order is the design, not an implementation detail** (elide oldest tool output → drop oldest whole turns → file listing → memories → the turn's own message); the stack contract is never dropped, and `estimatedTokens <= budget` is absolute because step 5 exists. **A tool call and its result must leave together or the request 400s** — output is elided in place rather than deleted; see CLAUDE.md. Budget is a *cost* limit (120k) far below the 1M window, on a ~4 chars/token local estimate. Stack contract sits first so the cacheable prefix is stable (Opus 5 caches from 512 tokens). `STACK_CONTRACT` is minimal pending M2-4, which expands it and adds the snapshot. All six guards verified by breaking them. |
| M2-4 | System prompt | M2-3 | DONE | `STACK_CONTRACT` → **`SYSTEM_PROMPT`** — it is the whole prompt now, and two constants where one is a prefix of the other drift apart. Sections: `<stack>` `<files>` `<scope>` `<response>`. **Reviewing the prompt against `packages/sandbox/template/` found two facts it had wrong:** it claimed a `public/` directory that does not exist, and said nothing about Tailwind v4 having no `tailwind.config.js` (v3 habits would create one and get silence). Both would have produced a *confident* wrong agent, not a confused one; both are now pinned by tests. **The absence of any self-verification instruction is load-bearing** and enforced by a phrase list plus `/\bverif/i` — see CLAUDE.md. `<response>` folds §4's conciseness requirement together with writing for a chat pane, since this model narrates more than its predecessors. Snapshot is of the **assembled** prompt (contract + file listing), so composition changes are caught too; first snapshot test in the repo — biome tolerates `.snap` with no config change. Prompt is ~600 tokens with a 900-token ceiling test. All four guards verified by breaking them. |
| M2-5 | Sandbox-proxy tools | M1-1, M0-3 | DONE | Six tools in `packages/agent/src/tools/`, jsdiff for the `file.changed` diff. **Two decisions a later task could otherwise undo:** `run_command` returns `ok: true` on a non-zero exit — a failing build is data the model must read, not a broken tool — and `shellQuote` moved to `@nap/shared/shell` because `search_files` interpolates a model-written grep pattern into a command line. `@nap/sandbox` is a devDependency of `@nap/agent` so tests use the real fake, unlike `@nap/context`'s local stub; `test/architecture.ts` reads only `dependencies`, so it stays green. Seven guards verified by breaking them. |
| M2-6 | Safety hooks | M2-5 | DONE | `packages/agent/src/safety/`. **The guard is enforced *inside* `executeTool`, not wrapped around it** — M2-5's comment said "the hooks that wrap this" and that was the wrong shape: at the chokepoint no caller can reach the shell around it, and a refusal comes back as an ordinary `tool.call` + `tool.result{ok:false}` the model reads and works with. **Matching is on the head of a shell segment, never a substring** — `ls && rm -rf /`, `$(curl …)`, backticks and `sh -c "…"` are all followed into, while `echo "run curl to fetch it"` still runs; see CLAUDE.md. Egress policy (user decision): package managers allowed because "add react-router" is an ordinary request, raw `curl`/`wget`/`ssh`/`git push` blocked, global installs blocked. Redirect targets go through the same path check as arguments, so `> /etc/x` lands on the machine rule rather than the project one. **`TurnBudget.check()` returns the `turn.failed` payload itself** rather than a flag — M2-7 emits what it is handed, so the reason cannot be re-derived differently at two call sites; defaults 40 steps / 400k tokens, and the limit is what may be spent, not the first thing refused. Ships tested and unhooked, like `NoopMemoryProvider` did. Four mutations verified: guard disabled → 33 fail; egress table emptied → 5 fail; budget always-ok → 6 fail; an over-eager bare-`rm` rule → the **allow** list fails, which is what proves that table is not vacuous. |
| M2-7 | `AgentService` | M2-1, M2-5, M2-6 | DONE | **Ordering tests green 10/10 consecutive runs, 16/16 each.** Built with M2-1 still IN_PROGRESS (user decision on spend) — safe because `AgentService` talks to the `LLMProvider` *port*, so a wrong request shape stays confined to `claude-provider.ts`; M2-9 verifies both. **The M0-4 port was extended a third time:** `AgentTurnRequest` gained `finalize?: () => Promise<{ commitSha }>` — `turn.completed` carries a commit sha but git is M2-8's, and always emitting `null` would make every turn claim it changed no files, since that is what `null` means in the schema. M2-8 passes its own `commitAll` closure; `AgentService` still imports no git. Turn state lives in a per-call `Turn` object, not on the service, so two concurrent turns cannot corrupt each other's message list or budget. **`TurnBudget` got its first consumer** — step check before `complete()` (so a doomed call is never paid for), token check after; M2-6's deferred "terminates the turn" assertion now has a real loop behind it. **Cancellation mid-batch abandons the collected results rather than sending them** — a user message answering only some of an assistant message's tool calls is a 400. **Known gap: nothing emits `agent.thinking`** — `LLMTurnResult` has no thinking field and adding one means editing the unverified `ClaudeProvider`; M3-4 (chat pane) is where this will bite. Four guards verified by breaking them. |
| M2-8 | `Runtime` | M2-7, M2-3, M1-5, M0-5 | DONE | All six §4 tests green, suite 10/10 consecutive runs. **A fourth port was needed:** `SessionStore` — a turn request carries only a session id, and resume-or-create needs the project and the current sandbox; two methods, Postgres impl deferred to M4-4. **A resume that fails fails the turn** (`sandbox_unavailable`) rather than creating a fresh sandbox: without M4-2's restore, starting over hands the user an empty template and calls it success. **`onEvent` is sync but `append` is not**, so the sink queues each event on a promise chain — that is what makes append-before-publish hold *and* orders the appends; `drain()` is the turn's durability point. Commit happens only inside M2-7's `finalize` hook, so "zero commits on failure" is structural rather than a branch. Ships the first `EventStore`/`EventBus` fakes (`packages/db/src/testing/`) plus `expectEventSequence`, which the `nap-events` skill had been describing before it existed. Six mutations verified, including one that proves the concurrency test is not vacuous. |
| M2-9 | CLI harness | M2-8 | IN_PROGRESS | **Built and working on fakes; the real run is deliberately outstanding** (user decision on spend). `bun run harness "<prompt>"` runs the whole composition on `ScriptedLLMProvider` + `InMemorySandboxManager` for free and prints the ordered stream; `--real` swaps in E2B and Claude. Options parsing and event formatting live in `src/harness.ts` — tested, and refusing an unknown flag, because a forgiving parser lets `--budget-tokns=100` spend the default on a real run. `ClaudeProvider` gained `model`/`effort`/`maxTokens` so a real run is Sonnet at `medium` with 12 steps / 40k tokens. `packages/runtime/tsconfig.json` had to learn about `scripts/`. **next step:** one `--real` run (M2 acceptance: real file changed, ordered events, real commit), batched with M2-1's never-executed `claude-provider.integration.test.ts` so both are paid for once. Nothing on the `--real` path — E2B construction, the credential check, sandbox teardown — has ever executed. |

## M3 — Presentation & Streaming

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M3-1 | `EventStore` (Postgres) + `EventBus` | M0-5 | DONE | **`seq` is assigned under `pg_advisory_xact_lock(hashtext(session_id))`**, with the next value derived inside the insert — not by a retry loop and not by a process-local counter. Unlocked `max+1` races under read-committed (proven: removing the lock fails the 100-parallel test and nothing else), and a counter in the process is wrong the moment a second API instance exists, whereas an advisory lock is cluster-wide. `unique(session_id, seq)` stays the backstop it was designed as; the code never reaches it, so there is no retry branch no test could cover. `seq` starts at **1** — `readFrom(sessionId, 0)` has to mean "everything". **Both methods read `created_at` back from the row** rather than echoing the caller's string, so a replayed event is byte-identical to the one published live; a non-canonical ISO input normalizes through `timestamptz` and only one of the two would have followed it. Rows are parsed through `NapEventSchema` on the way out. `InProcessEventBus` is a *new* production module (user decision), `node:events`-backed with max listeners disabled — one session has as many subscribers as open tabs, and the default 10 would warn per connection; the `testing/` fake is untouched, so M2-8 and the harness did not move. Deleted `packages/db/src/index.test.ts`, the placeholder that said it would go when the package got real modules. **Concurrency test green 10/10 consecutive runs.** Four mutations verified. Deviates from the task plan in one place: the free end-to-end smoke is a store+bus composition test (append→publish, replay lines up with the live tail) rather than the harness over Postgres, which would need the `SessionStore` Postgres implementation that M4-4 owns. |
| M3-2 | WebSocket endpoint | M3-1 | DONE | **Subscribe first, buffer, then replay** — the obvious order loses everything appended between the read and the subscribe, which is exactly the window a client connecting mid-turn lands in. One `lastSentSeq` gate on every send makes "no duplicates" structural; the *first* event is never gated, or a client asking for a seq past the end of the log is silenced forever and it looks like a working stream with nothing happening. **Two of my own tests were vacuous and only the mutations found it:** the in-flight-remainder test passed with the subscribe moved after the read, because its fake gated the read *before* the snapshot, so mid-turn events landed in the history anyway — it now snapshots first and gates the resolve; and "close unsubscribes" passed with `unsubscribe()` deleted, because the `closed` flag suppressed the send — a `CountingBus` now watches the subscription itself, and `vi.getTimerCount()` the heartbeat. Six mutations verified in total. Logic lives in `apps/api/src/ws/event-stream.ts` against a two-method socket type, because `hono/bun`'s `upgradeWebSocket` needs a live `Bun.serve` and Vitest runs under Node; `createApp` takes the adapter as a dependency. Frames are Zod'd in both directions (`@nap/shared/ws-protocol`), the heartbeat is an application-level `ping` (browser JS can neither send a control-frame pong nor see a ping), and a malformed frame gets an `error` frame with the socket left open. **`bun run ws:smoke` is a new free gate** — real Bun socket, in-memory store, 10 checks incl. replay-then-tail, pong-keeps-alive and a silent connection closed with 4000; all green. **Boot is wired for real:** `@nap/db/client`'s `createDatabase`, `PostgresEventStore` + `InProcessEventBus` in `index.ts`, and `websocket` added to the default export or an upgraded socket is never read. Verified by hand against a real Postgres — seeded 3 events, connected at `seq=1`, got exactly 2 back, `?sessionId=nope` → 400. Also fixed in passing: the request log line went through the raw logger, so it carried neither `requestId` nor `sessionId` — the two ids you would actually grep. `@types/bun` is now an `apps/api` devDep (`types: ["node","bun"]`) since it is the one workspace whose own code runs on Bun. |
| M3-3 | Client WS hook + reconnect | M3-2 | DONE | `useEventStream` in `apps/web/src/hooks/`, 17 tests, **six mutations verified** (reconnect from a hardcoded seq, no cap, no dedupe, no session filter, no teardown, no pong — each failed exactly the tests it should). **Backoff is deterministic**: 500ms doubling to a 10s cap, reset on `open` and *not* on the attempt, or a server refusing connections becomes a tight loop; no jitter, because jitter solves a thundering herd and a session is one browser tab. `lastSeq` lives in a ref — the reconnect URL needs the value as it is *now*, not as captured when the effect ran. The socket comes from an injected factory typed `interface StreamSocket extends EventTarget`, which a real `WebSocket` satisfies with no cast and a fake satisfies with `extends EventTarget`. `sessionId: undefined` connects to nothing, which is what the shell renders before sessions exist and what keeps `AppShell`'s tests off the network. **The status dot is wired** (`connection-status.tsx`, split into a presentational half the tests render and a live half that owns the subscription), reading `NEXT_PUBLIC_DEV_SESSION_ID` — **temporary, deleted by M3-7**. `apps/web` now depends on `@nap/shared` and needs `transpilePackages` in `next.config.ts`, since workspace packages resolve to raw TypeScript. **Known gap, deliberate:** nothing automated drives the hook against a live server. A live check under the `web` project is not possible — Node's global `WebSocket` (undici) throws `ERR_INVALID_ARG_TYPE` dispatching its own events into jsdom's `EventTarget`, before any handler runs. The halves are covered separately: 17 unit tests for the hook, `bun run ws:smoke` for the real socket path, `bun run build` for the bundling. What was checked by hand instead: `next dev` server-renders `connecting…` with `NEXT_PUBLIC_DEV_SESSION_ID` set against a real API and Postgres, and `no session` without it — proving the wiring and the env plumbing, not the socket. Closing that gap needs a browser, and belongs with M3-4 when there is a page worth opening. |
| M3-4 | Chat pane | M3-3, M0-3 | DONE | **Activity rail** (user decision): one hairline, every item hangs off it, the rail *is* the turn. Prose in sans, everything machine-authored in mono — that split is the type system and it says who wrote each line. `apps/web/src/chat/`: a pure `buildTranscript` fold (`.test.ts`, node — it needs no DOM) plus three components (`.test.tsx`). **All 11 types have a treatment, enforced structurally**: the table fails `toHaveLength(11)` *and* fails to compile if a type is added without a row — both verified by deleting one. Steps are `<details>`, so collapsing, keyboard support and the `group` role come from the platform; a **failed step opens itself**, because a failure you have to go looking for is one you miss. Status is words, not colour. **Three things the tests found rather than the plan:** a `tool.result` or `command.output` whose `tool.call` this client never received (the reconnect window) folded to *nothing* and silently hid a failed command — it now opens a step in place; `<pre>` with one text node made the clamp assertion vacuous, so output renders one element per line; and `role="log"` on a `<ul>` orphans `listitem`, so the container is a `div` and speakers are announced with `sr-only` text instead of `aria-label`. Also caught: `<summary>` maps to no `button` role here, so disclosures are queried as their `group`. Five behaviour mutations verified (result matched by `toolCallId` not recency, chunk append, running default, the clamp, a missing treatment). One new palette token, `--color-danger` — success stays unmarked. **Verified in a real Next build**, not only in jsdom: a temporary page server-rendered a full seeded turn and produced exactly the intended output — chips `+2 −1 changed`, the diff, the failed `run` step open with the last 12 of 20 lines behind "Show 8 more lines", the preview link and `Done · 18.4s · 12480 in / 1340 out · a1b2c3d`. Page deleted afterwards. `agent.thinking` now has a treatment and a test although **nothing emits it yet** (the M2-7 gap) — the first thing that does will be visible immediately. |
| M3-5 | Preview pane | M1-4 | DONE | **Nothing emitted `preview.ready`** — M1-4 added `waitForPreview` to the port and no caller ever appeared, so the pane as specified would have shown "starting" forever. The producer landed here (user decision): the runtime awaits `waitForPreview` after acquiring a sandbox and emits, **only when it created it**. A resumed sandbox is already serving and announced itself on the turn that made it, which the client replays; re-announcing would hard-reload the user's app on every turn, and Vite's HMR already handles the live case better. A preview timeout does **not** fail the turn — a slow dev server is no reason to refuse to edit the project. `previewPort`/`previewTimeoutMs` are options, defaulting to `TEMPLATE_DEV_PORT` and 20s. **A reload is a remount**: the frame is cross-origin so nothing here can call `location.reload()` on it; `key` is `${announcement seq}:${reload clicks}`, which is also why the pane knows the difference between a *new* announcement and the same one arriving again. Four states from a pure `previewState` fold — and two rules that are easy to get backwards: a preview already on screen **survives a later failure** (the app is still running), and only `sandbox_unavailable`/`internal` are preview failures, since `budget_exceeded`/`refusal`/`cancelled` are the agent stopping and the pane claiming the sandbox died would be a lie. Frame sandbox is `allow-scripts allow-same-origin …`: the app is model-written and served from its own origin, so `allow-same-origin` grants it nothing here while dropping `allow-scripts` would stop every React app in the template. **Two things the work exposed:** my first two reload tests passed for the wrong reason — the fixtures renumbered `seq` on every call, so the frame moved for reasons the test was not about (fixed with fixed events); and `test/comments.ts` caught a task ID I put in a runtime comment. `InMemorySandboxManager` gained `serves: [port]` — `listen()` cannot help a caller that creates the sandbox itself — which replaced a bespoke subclass in the runtime tests and makes `bun run harness` print `preview.ready` on the free path. Five mutations verified. **All four states rendered through a real Next build** (temporary page, deleted): idle, "Starting the dev server…", the sandboxed iframe with host/Reload/Open, and "The sandbox didn't start. / no capacity in us-east-1 / Send another message to try again." |
| M3-6 | File tree | M1-1 | DONE | **This is the first non-`/ws` HTTP surface** — a browser cannot hold a `SandboxManager`, so the tree and viewer are two read-only routes (`GET /sessions/:id/files`, `GET /sessions/:id/file?path=`) over `listProjectFiles` + `readFile`, tested end to end through `app.request()` against `InMemorySandboxManager`. **They ship unmounted at boot**: `AppDeps.files` is optional and `index.ts` has no `SessionStore`, so registering them anyway would answer a real request with a 500 from a missing dependency; M3-7 supplies it. One bounded walk per request rather than a call per opened folder (user decision) — a tree that fetched on expand could not reveal a file three levels down without a chain of round trips, and revealing it is the point. **The path parameter is the security-relevant line**: `ProjectPathSchema` refuses absolute paths, `..` segments, null bytes and non-canonical spellings, and the route tests assert **400 rather than 404** — a traversal that reached the filesystem would look like a missing file. `bytes` is real UTF-8 size, not string length. **A fourth statement of the project root appeared** (the browser needs it to match an event's absolute path against a relative listing) so `test/project-root.test.ts` now pins all four against each other — which needed `@nap/agent`/`@nap/context` as *root* devDependencies, since repo-level tests resolve from the root. Client is `apps/web/src/files/`: two pure folds (`.test.ts`, node), two hooks with an injected `fetch` (`.test.tsx`), and a `prism-react-renderer` viewer themed from `globals.css`. Changed files are marked in words and **their folders open themselves** — a mark inside a collapsed folder is one nobody reads. Nine mutations verified. **Rendered through a real `next build`** (temporary page, deleted): nested tree, two `changed` marks, the "some files are not shown" notice, and the viewer with numbered highlighted lines and "402 KB in total". |
| M3-7 | Turn submission wiring | M3-4, M2-8 | DONE | **The loop closes here** — boot composes the whole stack for the first time (sandbox manager, context engine, agent over `ClaudeProvider`, runtime, store, bus), so `E2B_API_KEY` becomes required and the API starts spending. **A wiring bug caught by reading rather than by a test:** a second `InProcessEventBus` for `/ws` type-checks and streams nothing; one store and one bus are shared, and nothing in the suite composes boot. **Env is platform-aware** (`NAP_PLATFORM=anthropic\|bedrock`) because requiring an Anthropic key from an AWS-billed setup is the check that teaches people to paste dummy values; model defaults to `claude-sonnet-5`/`medium` via `NAP_MODEL`/`NAP_EFFORT`. **POST answers 202 and the turn runs detached** — the client already has the story over the socket, and a 30–120s request loses to the first proxy with an idle timeout; the detached promise needs its `.catch` (an unhandled rejection ends the Bun process) and its `.finally` (or a late cancel aborts the *next* turn's signal). `TurnRegistry` is the only cross-request state in the API. **The optimistic message is held beside the transcript, never folded into it**: `buildTranscript` takes `StoredEvent`s and synthesizing one would mean inventing a `seq` and a `turnId`; it clears when the matching `user.message` arrives, and `running` is *derived from the log* so a reload mid-turn still shows a disabled input. **Two minimal slices of M4-4 landed** (user decision): `PostgresSessionStore` — note `sandbox_id` lives on `projects`, so sessions in one project share a sandbox — and `createProjectSession`, which is what finally deletes `NEXT_PUBLIC_DEV_SESSION_ID`; the browser keeps its session id in `localStorage`. Twelve mutations verified. **Proven against the real stack** (local Postgres, real boot, real socket): `POST /sessions` → ids, `POST /turns` → 202 → `user.message` + `preview.ready` from a **real E2B sandbox serving a real preview URL** → `turn.started`; and a second turn cancelled mid-flight → `turn.failed{cancelled}`. The model call itself 404s — the pre-existing Bedrock model-access block from T-12, not a wiring fault. Sandbox destroyed afterwards. |

## M4 — Persistence

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M4-1 | Snapshot on teardown | M1-5, M0-5 | TODO | |
| M4-2 | Restore on open | M4-1, M1-3 | TODO | |
| M4-3 | Idle reaper | M4-1 | TODO | |
| M4-4 | Project CRUD + list page | M0-5, M4-2 | TODO | |
| M4-5 | Full-cycle integration test | M4-2, M4-3 | TODO | |

## M5 — Auth & Hardening

| ID | Task | Deps | Status | Notes |
|----|------|------|--------|-------|
| M5-1 | Auth | M0-5 | TODO | |
| M5-2 | Authorization on every route | M5-1, M4-4 | TODO | |
| M5-3 | Rate limits & quotas | M5-1 | TODO | |
| M5-4 | Error surfaces | M3-4, M3-5 | TODO | |
| M5-5 | Observability baseline | M0-6 | TODO | |
| M5-6 | v1 acceptance run | all | TODO | record cost/latency per turn here |
