import { describe, expect, it } from "vitest";

// Placeholder proving the runner picks up this workspace. Replaced when the
// app is built out (web: M0-7, api: M0-6).
describe("@nap/web", () => {
  it("is wired into the test runner", () => {
    expect(true).toBe(true);
  });
});
